require 'spec_helper'

describe Keystores do
  it 'has a version number' do
    expect(Keystores::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
